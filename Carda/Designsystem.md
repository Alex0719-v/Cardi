# Carda Design System

> 本文件记录 Carda 的设计系统。当前依据用户描述与 Figma `交互名片 Copy` Page 1 建立。

---

## 1. 设计原则

- 以名片为视觉中心。
- 页面背景保持轻、灰、低干扰。
- 核心控件使用玻璃质感。
- 主色只用于强调当前状态、选中状态或关键交互。
- 名片本体需要清晰、克制、可保存为独立图片。
- 设计实现以 Figma frame 为准，不自行发明新的视觉语言。

---

## 2. Figma 基准

主要 Figma 文件：
- 文件名：`交互名片 Copy`
- Figma 链接：`https://www.figma.com/design/Vk8sBUsQh05R11tJMJPI32/%E4%BA%A4%E4%BA%92%E5%90%8D%E7%89%87--Copy-?node-id=0-1&t=fJIUVe3NlvYaytkV-1`
- File Key：`Vk8sBUsQh05R11tJMJPI32`
- Page：Page 1
- 以后仅使用该链接作为设计依据。

已读取的关键 frame：
- `7575:14550`：创建/编辑名片
- `9104:1302`：名片文字与信息栏校准参考
- `7597:15587`：我的名片，创建名片后
- `4268:13366`：无名片空状态
- `7636:17322`：点击头像后的底部弹窗
- `7723:15761`：我的名片长按菜单
- `7575:3207`：多名片分页状态与底部胶囊提示
- `7597:15120`：名片夹名字排序
- `7594:14921`：名片夹分类/单位模式
- `7648:3995`：名片夹展开名片
- `7648:4178`：名片夹长按菜单
- `4096:14211`：搜索入口展开
- `7648:3795`：搜索输入键盘态
- `4096:14285`：搜索结果分类

已定位但仍需后续补充细坐标的 frame：
- `7575:3280`、`7593:14599`：多名片横向分页状态

---

## 3. 颜色

当前 Figma 明确出现的颜色：

| Token | Value | 用途 |
| --- | --- | --- |
| `MainAccent` | `#FE793C` | 主色、选中强调 |
| `PageBackground` | `#F9F8FB` | 所有界面的统一页面背景 |
| `CardBackground` | `#FFFFFF` | 名片本体 |
| `PrimaryText` | `#000000` / `#1A1A1A` | 标题、主文字 |
| `SecondaryText` | `rgba(0,0,0,0.75)` | 名片内辅助信息 |
| `FormFill` | `rgba(120,120,120,0.2)` | 创建/编辑页灰色表单块 |
| `FormSecondaryText` | `rgba(60,60,67,0.6)` | 表单输入值/占位信息 |
| `FormGroupSeparator` | `#E6E6E6` | 创建/编辑页表单组件分隔线，来自 Figma Row `_Separator` / `separators/vibrant` |
| `Separator` | `#E6E6E6` | 表单分隔线、加减按钮内部线 |
| `DeleteRed` | Figma 红色圆形减号 | 删除字段按钮 |
| `GlassFill` | `rgba(255,255,255,0.65)` + blend layers | 玻璃质感控件 |
| `PageIndicatorFill` | `#D9D9D9` | 多名片分页胶囊背景 |
| `PageIndicatorActiveDot` | `#FFFFFF` | 多名片分页当前圆点 |
| `PageIndicatorInactiveDot` | `#EDEDED` | 多名片分页非当前圆点 |

维护规则：
- 新增颜色必须先确认用途。
- 主色 `#FE793C` 不应用作大面积背景。
- 我的名片、名片夹、名片搜索、创建/编辑名片和弹窗背景统一使用 `PageBackground` / `#F9F8FB`。
- 页面背景、玻璃控件、名片背景应保持低噪声。

---

## 4. 字体

当前 Figma 样式：

| 样式 | 字体 | Size | Line height | Weight | 用途 |
| --- | --- | --- | --- | --- | --- |
| H1 | PingFang SC | 34 | 41 | Bold / Semibold | 页面标题、编辑页姓名 |
| CardName | PingFang SC | 33 | 35 | Semibold | 名片姓名 |
| CaptionCN | PingFang SC | 15 | 20 | Regular | 表单字段标题 |
| BodyCN | PingFang SC | 17 | 22 | Regular | 表单中文内容 |
| CardBodyCN | PingFang SC | 14 | 20 | Regular / Medium | 名片职位、公司、地址 |
| CardBodyEN | SF Pro | 14 | 20 | Regular | 名片英文名、电话、邮箱 |
| ToolbarText | SF Pro | 17 | 22 | Semibold / Medium | 系统控件与导航 |

实现规则：
- 中文文本必须使用苹方 / PingFang SC。
- 英文、拼音、电话、邮箱、链接等拉丁字符信息必须使用 SF Pro。
- SwiftUI 实现中，中文入口优先使用 `CardaTheme.pingFang(...)`，英文入口优先使用 `CardaTheme.sfPro(...)`。
- 不新增自定义字体，除非用户确认。
- Figma 中部分 Apple 模板文本带有系统字距设置；实现时优先使用系统默认字距，不手动添加负字距，除非用户明确要求像素级覆盖。
- 名片中除姓名外的所有文字，当前统一按公司信息条样式处理：14pt、Regular；中文用 PingFang SC，英文/拼音/电话/邮箱/链接用 SF Pro。
- 名片左侧职位、中文姓名、英文名/拼音三项整体上移 19pt。中文姓名当前为 33pt Semibold；组内相对上一版上移 1.5pt。英文名/拼音仍为 14pt SF Pro Regular；组内相对上一版上移 5pt。

---

## 5. 页面级布局

基准主页面尺寸：
- 402 x 874

状态栏：
- y=0
- h=62

顶部标题区：
- y=62
- h=54

主页面标题：
- x=16
- y=62
- w=370
- h=41
- 字号 34

右上角用户头像按钮：
- 玻璃质感圆形。
- 未登录显示空白玻璃样式。
- 登录后显示用户头像。

底部导航：
- y=779
- h=95
- 左侧胶囊导航主体高度 54。
- 搜索按钮为独立圆形，尺寸 54 x 54。
- 激活搜索时，搜索圆形展开成横向搜索栏，左侧导航胶囊收缩为单按钮。

底部导航普通态：
- 左侧导航：x=25, y=16, w=184, h=54，相对底部导航栏。
- 左侧玻璃背景：x=-4, y=-4, w=192, h=62，相对左侧导航。
- 右侧搜索按钮：x=323, y=16, w=54, h=54，相对底部导航栏。
- 搜索按钮玻璃背景：x=-4, y=-4, w=62, h=62，相对搜索按钮。

底部导航图标来源：
- 左侧“我的名片”和右侧“名片夹”图标不再由 SwiftUI 手绘。
- 图标文件存放在项目 `My icon/` 文件夹中，并由底部导航代码直接读取。
- `mycard-1.svg`：我的名片未选中。
- `mycard-2.svg`：我的名片选中。
- `cards-1.svg`：名片夹未选中。
- `cards-2.svg`：名片夹选中。
- 当前垂直校准：`mycard-*` 图标相对原始居中位置上移 3px；`cards-*` 图标相对原始居中位置上移 3px。
- 若后续替换图标，应优先替换 `My icon/` 中的源文件，并同步更新本文档。

底部导航搜索展开态：
- 左侧收缩按钮：x=25, y=16.25, w=53, h=53.5。
- 搜索栏：x=103, y=16, w=279, h=62。
- 键盘态搜索栏：x=16, y=464, w=293, h=62。
- 键盘态取消按钮背景：x=325, y=460, w=61, h=62。

---

## 6. 名片视觉系统

### 6.1 名片基准

来源：Figma `7597:15587`

| 属性 | 值 |
| --- | --- |
| 位置 | x=16, y=157 |
| 尺寸 | 370 x 222 |
| 圆角 | 24 |
| 背景 | white + `Card photo/Group 42.png` |
| 阴影 | `0 0 4 rgba(0,0,0,0.25)` |
| 底图图片 | 370 x 300，居中裁切 |
| 覆盖层 | 无，不再叠加半透明图层 |

名片横向宽度固定为 370pt。短边基础高度为 222pt，随信息量增长。
名片底图不再使用 SwiftUI 自绘椭圆或渐变，必须直接读取项目 `Card photo/Group 42.png`，按名片区域 `scaledToFill` 居中并上下裁切。底图上方不得再叠加半透明柔光、灰色遮罩或其他覆盖层。

### 6.2 名片内部坐标

以下为相对名片左上角的基准坐标：

| 元素 | x | y | w | h |
| --- | --- | --- | --- | --- |
| 公司 logo | 23 | 34 | 28 | 28 |
| 公司名称 | 56 | 38 | 28 | 20 |
| 头像 | 291 | 19 | 60 | 60 |
| 职位 | 23 | 80 | 28 | 20 |
| 姓名 | 23 | 当前实现基准上移 1.5 | 132 | 35 |
| 英文名/拼音 | 23 | 当前实现基准上移 5 | 55 | 20 |
| 信息文本列 | 127 | Figma 基准 + 6 | 约 185 | auto |
| 信息图标列 | 330 | Figma 基准 + 6 | 20 | 20 |

### 6.3 动态高度

```text
cardHeight = 222
  + max(0, visibleInfoFieldCount - 3) * 24
  + wrappedExtraLineCount * 20
```

锁定关系：
- 左上角位置锁定。
- 横向宽度 370 锁定。
- 公司组顶部锁定。
- 头像顶部和右侧锁定。
- 姓名组基于名片高度竖向居中后整体上移 19pt，组内元素使用固定坐标微调。
- 右下角信息模块底部锁定。

### 6.4 可选元素

头像：
- 有头像：显示 60 x 60 圆形头像。
- 无头像：删除头像圆形，不显示占位。

公司 logo：
- 有 logo：显示 28 x 28 logo，公司名 x=56。
- 无 logo：不显示 logo，公司名移动到 x=23。

信息字段：
- 支持手机、邮箱、地址、链接。
- 图标自动匹配字段类型。
- 手机、邮箱、地址图标不再由 SwiftUI 手绘，必须读取项目 `My icon/` 文件夹中的 `Phone.svg`、`Mail.svg`、`Map pin.svg`。
- 链接图标暂保留 SwiftUI 线条绘制；如果后续用户提供 `Link.svg`，再改为本地资源。
- 文字点击可复制。
- 图标点击可打开对应系统软件。

### 6.5 多名片分页胶囊

来源：Figma `7575:3207`

| 元素 | x | y | w | h |
| --- | --- | --- | --- | --- |
| 胶囊背景 | 171 | 716 | `count * 20` | 20 |
| 圆点 | 以胶囊内 HStack 排列 | 722 | 8 | 8 |

规则：
- 只有多于 1 张我的名片时显示。
- 胶囊背景为 `#D9D9D9`，圆角 100。
- 当前圆点为 `#FFFFFF`，非当前圆点为 `#EDEDED`。
- 圆点直径 8pt，圆点间距 12pt。
- 不使用阴影，不使用液态玻璃背景。
- 每增加 1 张名片，胶囊宽度增加 20pt，并增加 1 个圆点；减少时反向处理。

---

## 7. 创建/编辑表单

来源：Figma `7575:14550`

页面尺寸：
- 402 x 1044

顶部预览名片：
- x=16
- y=63
- w=370
- h=222
- cornerRadius=24

头像上传入口：
- x=43
- y=240
- w=87
- h=90

姓名：
- x=16
- y=340
- w=210
- h=41
- 字号 34
- 默认占位文本：`姓        名   `
- 输入文本与占位文本都保持大字距，当前实现 tracking=8。

英文名/拼音：
- x=16
- y=385
- w=247
- h=22
- 字号 17
- 默认占位文本：`Xing Ming`
- Figma 字距：16px；实现中 tracking=16。

表单块：
- 公司/职位组：x=16, y=425, w=370
- 公司/职位/LOGO 组必须作为独立固定组件实现，不得复用动态信息字段行组件；该组的文字定位、点击层和分隔线规则与动态字段不同。
- 公司/职位组高度：204，三行各 68；三行必须按显式 y=0/68/136 放置，避免分隔线、图片选择器或自动布局把 LOGO 行挤偏。
- 公司行：相对公司/职位组 y=0，标题 x=16 y=13，输入/占位 x=16 y=35；标题 w=338 h=22，输入/占位 w=338 h=20。
- 职位行：相对公司/职位组 y=68，标题 x=16 y=13，输入/占位 x=16 y=35；标题 w=338 h=22，输入/占位 w=338 h=20。
- LOGO 上传行：相对公司/职位组 y=136，标题 x=16 y=13，副标题 x=16 y=35，右侧箭头 x=334 y=29。
- LOGO 上传行必须先绘制可见内容，再覆盖透明的图片选择点击层；不能把可见内容直接放进 PhotosPicker label 后再依赖其布局。
- 创建/编辑页所有表单组件分隔线左右必须与组件边缘留出相等间隔；当前左右各 16pt，分隔线 x=16，w=338，h=1。
- 上方公司/职位/LOGO 组和下方动态字段组的分隔线必须使用同一颜色：`FormGroupSeparator` / `#E6E6E6`。
- 公司/职位/LOGO 组分隔线必须为独立绘制：x=16，y=68/136，w=338，h=1。
- 动态字段组：x=16, y=654, w=370，高度为所有动态字段行高度之和。
- 圆角：26
- 背景：`rgba(120,120,120,0.2)`
- 动态字段行基础高度：68。
- 动态字段组内分隔线：x=16，w=338，h=1；y 坐标跟随每一行的实际累计高度。
- 新增 1 个动态字段，总高度增加 68；删除 1 个动态字段，总高度减少 68。
- 动态字段文本每多换 1 行，该小单元行高增加 20，同时动态字段组总高度增加 20。
- 动态字段行 y 坐标由前面所有行的实际高度累加得到，不使用固定 y=0/68/136/204 表。
- 新增字段加号位于动态字段组底部下方 42pt，即 `y = 654 + dynamicFieldsHeight + 42`；默认四栏时对应 Figma x=187, y=968。

编辑页默认占位文案：
- 公司：`请输入文本`
- 职位：`请输入文本`
- LOGO：`上传公司LOGO`
- 手机：`请输入手机号`
- 邮箱：`请输入邮箱`
- 地址：`请输入地址`
- 链接：`https：//`

字段操作：
- 字段类型右侧只保留一个向下小箭头，表示可选择类型。
- 动态字段类型只包含：手机、邮箱、地址、链接；`公司logo` 不出现在该选择菜单中。
- 公司 LOGO 只通过公司/职位组中的固定 `LOGO` 行上传。
- 红色圆形减号表示删除字段；Figma `7575:14550` 中默认四行动态字段只有后三行显示删除圆圈，首行手机不显示删除圆圈。
- 删除圆圈尺寸 28 x 28，颜色 `#FF1212`，内部减号为 `#E6E6E6` 12 x 2。
- 底部灰色圆形加号表示新增字段；x=187, w=28, h=28，y 随动态字段组高度变化；圆形颜色 `#787878`，横线 `#E6E6E6` 12 x 2 且位于圆内 x=8 y=13，竖线 `#E6E6E6` 2 x 12 且位于圆内 x=13 y=8。
- 左上角返回按钮只返回上一页面，不创建也不修改名片。
- 右上角确认按钮才会提交创建或修改。

---

## 8. 空状态与列表组件

### 8.1 我的名片空状态

来源：Figma `4268:13366`

| 元素 | x | y | w | h |
| --- | --- | --- | --- | --- |
| 空卡片 | 16 | 158 | 370 | 223 |
| 加号横线 | 185 | 268 | 32 | 4 |
| 加号竖线 | 199 | 285 | 4 | 32 |

空卡片点击后进入创建/编辑名片。

### 8.2 折叠名片

用于名片夹和搜索结果。

| 元素 | x | w/h | 规则 |
| --- | --- | --- |
| 折叠名片背景 | x=16 | w=370, h=60 | 圆角 30，只使用不透明纯白色和完整名片同款阴影 `0 0 4 rgba(0,0,0,0.25)`，y 随列表位置变化 |
| 头像 | x=334 | 44 x 44 | 有头像显示头像，无头像隐藏，不显示灰色圆形占位 |
| 单位文本 | x=41 | h=20 | 当前距卡片顶部 5.5pt |
| 姓名文本 | x=41 | h=22 | 当前距卡片顶部 30.5pt |

同一分组内折叠名片 y 方向基准间距为 68pt，即 60pt 高度 + 8pt 间距。
上一分组最后一张名片到底下一分组第一张名片之间为 45pt。
折叠名片效果代码不得包含液态玻璃、材质、blend、描边、遮罩或额外 overlay；除圆角外，只保留不透明纯白填充和完整名片同款阴影 `0 0 4 rgba(0,0,0,0.25)`。

### 8.3 名片夹顶部分类栏

来源：Figma `7594:14921`、`7597:15120`

| 元素 | x | y | w | h |
| --- | --- | --- | --- | --- |
| 顶部背景 | 0 | 0 | 402 | 160 |
| 标题栏 | 0 | 63 | 402 | 54 |
| `名片夹` 标题 | center x=201 | 76 | 约 80 | 28 |
| 列表标签 | 41 | 137 | 30 | 20 |
| 姓名标签 | 186 | 137 | 30 | 20 |
| 单位标签 | 331 | 137 | 30 | 20 |
| 灰色横线 | 0 | 159 | 402 | 1 |
| 列表选中线 | 43 | 159 | 26 | 1 |
| 姓名选中线 | 188 | 159 | 26 | 1 |
| 单位选中线 | 333 | 159 | 26 | 1 |
| 字母索引 | 389 | 52 | 13 | 770 |

名片夹姓名模式、单位模式内容区背景与顶部分类栏一致，统一使用 `PageBackground` / `#F9F8FB`。
姓名模式与单位模式右上角头像复用“我的名片”页的当前用户头像玻璃按钮；切换姓名/单位/列表模式时不得改成名片夹联系人头像。
姓名模式与单位模式底部使用实时 backdrop blur material 渐变效果：单层 `UIVisualEffectView` + `UIBlurEffect(style: .systemUltraThinMaterial)` 作为实时取样背后内容的模糊底层，再通过渐变 mask 控制透明过渡；不叠加颜色填充，不使用旧的 repetitions 叠层或遍历清理系统内部背景层。

列表模式顶部按钮：
- `编辑` 胶囊：x=16, y=67, w=72, h=46；文字 x=35, y=79，PingFang SC Medium 17pt。
- `添加列表` 胶囊：x=274, y=67, w=112, h=46；文字 x≈296, y=79，PingFang SC Medium 17pt。
- 按钮阴影使用淡阴影，当前为上一版强度的 1/2。
- 最顶上的列表行上方不显示分隔线；第二行及后续列表行上方显示分隔线。
- 列表模式默认显示 `同学（20）`、`xx讲座（15）`、`招聘（6）`、`见面会（12）` 四行 grouped table；点击列表行后，名片在该行下方展开。

名片夹无数据空状态：
- 去除图标，仅显示 `暂无收到的名片`。
- 文本需要位于 402 x 874 页面横向与纵向正中心。

示例数据导入：
- `/Users/alex/Desktop/联系人信息.xlsx` 的 30 条联系人用于名片夹测试。
- 启动时按“姓名 + 手机号”补齐缺失示例，避免重复插入。

### 8.4 名片搜索

来源：Figma `4096:14211`、`7648:3795`、`4096:14285`

搜索入口展开态：
- 顶部标题为 `最近添加`，x=16, y=71, h=28，PingFang SC Semibold 22pt。
- 不使用 `ScreenHeader` 的 34pt 大标题，也不显示 `搜索` 文本。
- `最近添加` 标题 zIndex 必须高于 ScrollView、顶部模糊和折叠名片，防止折叠名片上滑遮住标题。
- 顶部增加一层实时 backdrop blur material 渐变效果：x=0, y=0, w=402, h=119，mask 从顶部不透明到下方透明；模糊层位于标题下一层，不叠加颜色填充。
- 最近添加折叠名片从 y=119 开始，x=16, w=370, h=60，后续 top-to-top 间距 68pt。
- 最近添加只显示收到或保存到名片夹的名片，排除自己的名片。
- 折叠名片点击后在原列表位置展开为完整名片，再次点击展开名片收起。
- 左侧收缩菜单按钮：点击区 x=25.5, y=16.25, w=53, h=53.5；可视玻璃圆形 x=21.5, y=12, w=62, h=62。
- 搜索栏普通展开态：x=103, y=12, w=279, h=62；相对 Figma 初版上移 4pt，以匹配用户最新校准。
- 搜索栏普通展开态必须覆盖整条透明点击层，点击任意位置进入输入态并聚焦 TextField。

搜索键盘态：
- 底部导航栏整体上移到 y=448。
- 左侧菜单按钮消失。
- 输入搜索栏 x=16, y=464, w=293, h=62。
- 取消圆圈 x=325, y=460, w=61, h=62，点击后收起键盘并保留搜索页/搜索文字。
- 实时 backdrop blur material 渐变层跟随键盘上移，top=433, h=140，不叠加颜色填充。
- TextField 获取焦点时必须同步进入键盘态；如果模拟器连接硬件键盘，系统可能只显示候选条，但真机和未连接硬件键盘时应显示系统软键盘。

搜索结果态：
- 顶部标题为 `名片搜索`，center x=201, y=90, h=28，PingFang SC Semibold 22pt。
- 分组标题使用 PingFang SC Regular 15pt，黑色。
- `姓名` 标题 x=16, y=130, h=20；第一条结果 x=16, y=162。
- `单位` 标题 x=16, y=235, h=20；第一条结果 x=16, y=267。
- 搜索结果中的折叠名片同样支持点击原位展开和再次点击收起。

### 8.4 展开名片

来源：Figma `7648:3995`

| 元素 | x | y | w | h |
| --- | --- | --- | --- | --- |
| 展开名片 | 16 | 350 | 370 | 222 |
| 底部导航栏 | 0 | 779 | 402 | 95 |

展开名片的内部布局复用完整名片规则。
在名片夹中，展开名片必须占据原折叠名片所在的列表位置并挤开上下名片，不允许用浮层覆盖列表。

---

## 9. 搜索组件

### 9.1 搜索入口展开

来源：Figma `4096:14211`

- 最近添加列表从 y=119 开始。
- 最近添加不显示自己的名片，只显示收到或保存到名片夹的名片。
- 折叠名片 x=16, w=370, h=60。
- 每条折叠名片 top-to-top 间距为 68pt。
- 收缩导航按钮 x=25, y=16.25, w=53, h=53.5。
- 展开搜索栏 x=103, y=16, w=279, h=62。
- 搜索页底部使用实时 backdrop blur material 渐变效果，不叠加颜色填充，也不使用旧的重复叠层增强模糊。

### 9.2 搜索键盘态

来源：Figma `7648:3795`

| 元素 | x | y | w | h |
| --- | --- | --- | --- | --- |
| 搜索底部区域 | 0 | 448 | 402 | 95 |
| 输入搜索栏 | 16 | 464 | 293 | 62 |
| 取消按钮背景 | 325 | 460 | 61 | 62 |
| 键盘 | 1 | 532 | 400 | 342 |

键盘态实时 backdrop blur material 渐变层移动到搜索栏所在底部区域上方，不保留在被键盘遮挡的屏幕底部。

### 9.3 搜索结果

来源：Figma `4096:14285`

| 元素 | x | y | w | h |
| --- | --- | --- | --- | --- |
| 姓名分组标题 | 16 | 130 | 30 | 20 |
| 姓名结果首条 | 16 | 162 | 370 | 60 |
| 单位分组标题 | 16 | 235 | 30 | 20 |
| 单位结果首条 | 16 | 267 | 370 | 60 |
| 搜索底部区域 | 0 | 448 | 402 | 95 |
| 键盘 | 1 | 532 | 400 | 342 |

---

## 10. 弹窗与菜单

### 10.1 添加名片 Sheet

来源：Figma `7636:17322`

| 元素 | x | y | w | h |
| --- | --- | --- | --- | --- |
| 遮罩 | 0 | 0 | 402 | 874 |
| Sheet | 0 | 409 | 402 | 812 |
| 添加名片入口 | 16 | 462 | 370 | 52 |
| 账户信息卡片 | 16 | 550 | 370 | 100 |
| 账户头像 | 35 | 574 | 52 | 52 |
| Action Group | 16 | 687 | 370 | 208 |

### 10.2 我的名片长按菜单

来源：Figma `7723:15761`

| 元素 | x | y | w | h |
| --- | --- | --- | --- | --- |
| 名片 | 16 | 157 | 370 | 222 |
| Context Menu | 76 | 395 | 250 | 146 |

菜单规则：
- 三行按钮：`编辑名片`、`保存为图片`、`删除名片`。
- 每行高度 42，菜单上下内边距 10。
- 字体：PingFang SC Regular 17pt，line height 22pt。
- 文本居中；普通文本为 `#1A1A1A`，删除为 `#FF383C`。
- 圆角 34，使用液态玻璃质感；不显示右侧系统图标。
- 菜单出现时需要有透明的全屏点击层；点击菜单外空白区域关闭菜单，点击菜单按钮仍执行对应操作。

### 10.3 名片夹长按菜单

来源：Figma `7648:4178`

| 元素 | x | y | w | h |
| --- | --- | --- | --- | --- |
| 遮罩 | 0 | 0 | 402 | 874 |
| 展开名片 | 16 | 350 | 370 | 222 |
| Context Menu | 76 | 602 | 250 | 104 |
| 底部导航栏 | 0 | 779 | 402 | 95 |

交互规则：
- 菜单出现时需要有透明的全屏点击层。
- 点击菜单外空白区域关闭菜单。
- 点击菜单内部按钮不关闭透明层的空白点击逻辑，而是执行按钮自身操作。

---

## 11. 组件清单

当前需要沉淀为 SwiftUI 组件的设计对象：

- `UserAvatarButton`：右上角玻璃头像按钮。
- `BottomNavigationBar`：底部胶囊导航。
- `SearchDockButton` / `SearchBarDock`：搜索圆球与展开搜索栏。
- `BusinessCardView`：完整名片渲染。
- `BusinessCardPreview`：编辑页顶部预览名片。
- `CardEditorView`：创建/编辑名片页。
- `EditableCardFieldRow`：可输入、可换类型、可删除的信息行。
- `CardCarousel`：我的名片多卡循环横滑。
- `PageIndicatorCapsule`：多名片胶囊翻页提示。
- `CollapsedContactCardRow`：名片夹折叠横条名片。
- `ExpandedContactCardView`：名片夹展开名片。
- `ContextActionMenu`：长按后的选项框。

---

## 12. 维护规则

- 名片相关视觉规则优先写入 `AGENTS.md` 与本文。
- 新增颜色、字体、组件或布局规则后，同步更新本文。
- 如果 Figma 与文档冲突，先询问用户，不直接选择其一。
- 不因单个页面临时需求随意创建不可复用样式。
