# Cardi Roadmap

> 本文件记录 Cardi 的开发路线。路线来自用户描述与 Figma `交互名片 Copy` Page 1。

更新时间：2026-07-24

---

## 状态标记

- `[done]` 已完成
- `[doing]` 进行中
- `[todo]` 未开始
- `[blocked]` 等待确认
- `[research]` 技术验证中

---

## 已完成

1. 创建基础 SwiftUI 项目结构 `[done]`
2. 配置 SwiftData 示例容器 `[done]`
3. 创建项目指导文件结构 `[done]`
3.1 将用户品牌、Xcode 工程、Target、Scheme、构建产品、Swift 模块、App 入口和测试 Target 统一改名为 `Cardi`；Bundle ID 与本地账户目录保留兼容旧名称 `[done]`
3.2 接入 `AppIcon.icon/` Icon Composer 应用图标，保持 Target 的 `AppIcon` 名称与现有 Bundle ID，并完成 Debug、Release 构建及模拟器主屏幕显示验证 `[done]`
4. 明确产品方向：社交类交互名片软件 `[done]`
5. 记录主要功能：名片创建、名片交换、名片管理 `[done]`
6. 记录核心页面：我的名片、名片夹、名片搜索 `[done]`
7. 记录名片生成与布局规则 `[done]`
8. 将 Figma 设计来源切换到 `交互名片 Copy` `[done]`
9. 补充空状态、搜索三态、名片夹列表/姓名/展开/长按、添加名片 Sheet 的本地文档坐标 `[done]`
10. 建立第一版 SwiftUI / SwiftData 业务原型 `[done]`
11. 完成我的名片、创建/编辑、名片夹、搜索入口和 PNG 保存的初版串联 `[done]`
12. 底部导航“我的名片 / 名片夹”图标改为读取 `My icon/` SVG 文件 `[done]`
12.1 底部导航按 Figma `10063:3134` 的填充、折射、高光描边与阴影参数还原为原生 Liquid Glass 组合，多个玻璃表面共享 `GlassEffectContainer`，选中态保留原本的灰色滑块 `[done]`
12.2 底部 tab 切换加入 SVG 缩小—放大—回稳与整条 Liquid Glass 底栏轻量压缩回弹反馈，并适配“减弱动态效果” `[done]`
12.3 增强全局 Liquid Glass 的折射、融合范围与悬浮深度，并将底部灰色滑块改为参考登录/注册 segmented control 的 0.24 秒零回弹平滑切换 `[done]`
12.4 将底部导航玻璃容器的静止融合距离收紧为 0，修复进入名片搜索页时搜索框自动吸引、形变旁边组件；保留真实按压的 interactive glass 反馈 `[done]`
13. 建立代码与 `.md` 文档同步规则 `[done]`
14. 确认全局字体规则：中文 PingFang SC，英文 SF Pro `[done]`
15. 我的名片长按菜单按 Figma `7723:15761` 改为三按钮玻璃菜单 `[done]`
16. 名片底图改为读取 `Card photo/Group 42.png` 并居中裁切 `[done]`
17. 名片手机、邮箱、地址、链接图标改为读取 `My icon/` 中的 SVG 文件 `[done]`
18. 名片姓名字号放大 6pt，非姓名文字统一为公司信息条样式 `[done]`
19. 多名片分页胶囊按 Figma 更新尺寸并删除阴影；颜色已同步新版 `10063:3278...3281`：背景 `#CFCED3`、当前点 `#FFFFFF`、非当前点 `#D8D7DB` `[done]`
20. 创建/编辑页改为返回不保存、确认才提交 `[done]`
21. 创建/编辑页字段位置、默认占位文案、姓名/拼音字距与配色按最新 Figma `10599:4144` 校准 `[done]`
22. 创建/编辑页 LOGO 行改为显式定位，动态信息栏按字段数量和换行数自适应高度 `[done]`
23. 创建/编辑页公司/职位/LOGO 固定信息组整体重写，文字坐标独立定位，不复用动态字段行组件 `[done]`
24. 创建/编辑页表单分隔线统一为左右等距留白，并统一上下组件分隔线颜色 `[done]`
25. 名片成品旧版排版完成一轮像素校准；后续已迁移到 Figma `9978:2322` 新版名片规则 `[done]`
26. 名片成品左侧职位、姓名、英文名/拼音组完成旧版校准；后续已迁移到 Figma `9978:2322` 新版名片规则 `[done]`
27. 移除名片底图上方的半透明柔光覆盖层 `[done]`
28. 我的名片长按菜单支持点击菜单外空白处关闭 `[done]`
29. 名片夹顶部栏按 Figma 重写，补齐分类灰线、三态黑线和列表模式顶部按钮 `[done]`
30. 从 `/Users/alex/Desktop/联系人信息.xlsx` 接入 30 条名片夹示例数据，按“姓名 + 手机号”补齐缺失项并避免重复；该开发期数据已在发布清理项中退役 `[done]`
31. 默认页面背景统一为 `PageBackground` / `#F9F8FB`，并记录新版 Figma 例外背景 `[done]`
32. 修复创建/编辑页姓名占位截断，并固定姓名与英文名/拼音单行输入栏不可上下拖动 `[done]`
33. 补齐名片信息换行高度计算：每多 1 行名片高度增加 20pt，右下角信息模块保持底部锚定 `[done]`
34. 优化我的名片多卡左右滑动方向动画，并为长按菜单加入出现/消失动画 `[done]`
35. 将创建/编辑页姓名默认占位字距缩小为上一版的 1/2，并将“名”字累计左移 43pt；姓名与英文名空输入框聚焦时占位透明度改为 40%，英文名仅占位保留 16pt 字距，实际输入恢复正常字距 `[done]`
36. 将我的名片多卡滑动改为跟手拖动，相邻名片保持 32pt 间距并降低松手补全延迟 `[done]`
37. 将所有右上角头像入口统一为登录账户头像数据源，不再使用名片头像 `[done]`
38. 将底部导航与名片 SVG 图标从 `WKWebView` 异步加载改为本地缓存解析 + SwiftUI `Canvas` 同步渲染，减少页面切换和名片滑动时的图标闪烁或延迟 `[done]`
39. 核对 Figma 滚动边缘效果的渐进模糊、常规模糊与线性 mask 结构 `[done]`
40. 将最近添加页面的输入态搜索文本框上移 4pt，从 y=464 调整为 y=460 `[done]`
41. 恢复名片夹与搜索页此前移除的全部渐变毛玻璃层，并将实时背景模糊设为满强度 `[done]`
42. 名片夹列表模式加入唯一列表归属、底部灰色未分类列表和真实动态计数 `[done]`
43. 按 Figma `9522:1321` 完成添加列表 Alert，支持空名称禁用、创建置顶与取消 `[done]`
44. 列表展开保留从当前列表行后方向下滑出的位移动画，并在滚动到二级导航底边时吸顶 `[done]`
45. 将创建/编辑页与搜索框的换行键改为确认/完成键，提交后释放焦点并主动收起键盘 `[done]`
46. 为名片夹姓名/公司模式加入分组索引吸顶及旧索引渐隐交接；原 10pt 规则已由第 113 项的 2.5pt→中心透明度映射取代 `[done]`
47. 将新版名片夹姓名/公司模式滚动视口上沿移至 y=164，并以 24pt 透明安全区保持首个分组标题在 y≈188 `[done]`
48. 新版名片夹按 Figma `10063:3011` / `10064:3390` / `10064:3656` 隐藏旧顶部 Soft 渐变毛玻璃，底部保留 140pt 最大渐变背景模糊且不叠加额外实色色块 `[done]`
49. 将渐变毛玻璃色调统一为 50% `PageBackground`，并把右侧字母索引提升到上下毛玻璃之上 `[done]`
50. 为名片夹右侧 A-Z/# 索引加入点击与滑动检索，姓名与公司模式均定位对应字母分组，并补足末尾分组吸顶空间 `[done]`
51. 重写姓名/公司标题层：所有标题沿原生 Section 轨迹通过 Anchor Preference 绘制到前景层，并将首张名片间距缩小到新版 Figma 间距 `[done]`
52. 将底部左侧胶囊导航右边缘向左收回 0.5pt `[done]`
53. 将底部左侧胶囊导航右边缘继续向左收回 0.5pt，累计 1pt `[done]`
54. 将最近添加页左下角菜单圆球按钮整体向左移动 1pt `[done]`
55. 重构搜索清空逻辑：结果态叉号清空搜索并返回最近添加 `[done]`
56. 将搜索改为输入即检索，非空文本实时进入名片搜索并随输入变化刷新结果 `[done]`
57. 修复列表模式收起后的空白滚动偏移，并支持列表内折叠名片点击展开完整名片 `[done]`
58. 锁定应用浅色视觉环境，修复系统深色模式下渐变毛玻璃与部分 Liquid Glass 变为深灰/黑色 `[done]`
59. 修复编辑页动态字段确认键被纵向 TextField 解释为换行的问题 `[done]`
60. 手机号输入与名片展示统一按 `3 4 4` 自动插入空格，并兼容无空格搜索与拨号 `[done]`
61. 动态字段输入时自动滚动到键盘上方，结束输入后恢复原滚动位置 `[done]`
59. 将“我的名片”多卡底部分页胶囊整体下移 34pt `[done]`
60. 按新版 Figma 重写名片夹页面：新增浅灰大面板、按 Union path 绘制的列表/姓名/公司切换块、姓名/公司页 34pt 大标题和右上角账户头像；列表页按钮改为 `多选` / `添加列表` `[done]`
68. 统一“我的名片”与名片夹右上角账户头像：两处复用同一个先定 frame 再施加玻璃的 `AccountAvatarGlassSurface`，静态端点统一为 44 x 44 / r22 完全倒角正方形；未登录显示带阴影空白玻璃，登录后以 `scaledToFill` overlay 显示无背后阴影的账户头像；名片夹头像可打开同一账户 Sheet `[done]`
69. 将“我的名片”账户头像上移 2pt，与名片夹统一锁定在 y=62 `[done]`
61. 修正名片夹顶部切换块：选中项先拼合矩形再统一倒角，未选中标签按 Figma 三态固定映射镜像；删除底部菜单下层实色色块并将列表分隔线改为 `#C6C6C8` `[done]`
62. 修复名片夹底部遮挡：滚动视口延伸到画板底部，底部仅保留 y=737、h=140 的最大渐变背景模糊，折叠名片不再被 y=779 硬截断 `[done]`
62.1 修复名片夹原生 bottom Soft Edge 因 820pt 固定 ScrollView 延伸到屏幕外而不可见：恢复 y=737、h=140 的 `FigmaScrollEdgeSoftOverlay`，使用单一 Alpha Mask 和 Figma 双层背景模糊 / Screen 结构，同时保持滚动物理与 Sticky Header 不变 `[done]`
63. 按 Figma `10063:3262` 将“我的名片”页面背景改为 `#E1E0E3`，并去掉该页面完整名片本体外框阴影 `[done]`
64. 将名片夹顶部选中 Union、完整名片外框、右下裁切槽与折叠名片改为普通 `.circular` / 圆弧圆角，移除 Corner smoothing / `.continuous` 圆角语义 `[done]`
65. 按 Figma `10064:3390` 的 `渐变遮罩` 图层为名片夹姓名/公司模式加入顶部名片渐隐遮罩，并裁切 y=178 以上的名片内容 `[done]`
66. 按 Figma `10240:4332` / `10368:4034` 完成列表行长按菜单、修改列表名称 Alert 和删除列表原生确认；长按菜单已改为跟随目标列表行并显示在其下方 `[done]`
67. 按 Figma `10592:4167` / `9574:2114` 为名片夹折叠名片与展开完整名片加入左滑删除，删除前使用确认弹窗 `[done]`
68. 按更新后的 Figma `10592:4167` 将折叠态删除按钮改为 44pt 红色圆形、垃圾桶图标改为 24pt，并限制页面内仅一张名片可处于删除模式 `[done]`
69. 将名片夹与搜索页渐变模糊 tint 改为各自遮罩底部的真实背景色：名片夹跟随当前面板 50% / 65% 复合色，搜索页统一使用 `#D8D7DB` `[done]`
70. 为渐变模糊增加不透明端同色收束层，修复搜索键盘升起后系统材质仍把遮罩末端颜色改浅的问题 `[done]`
71. 按更新后的 Figma `9574:2114` 将展开名片左滑删除按钮改为上下各留 15pt、圆角 24pt，并把图标调整为 32pt `[done]`
72. 将名片夹折叠态与展开态的全部垃圾桶标识统一为新版 28pt `Trash 2` `[done]`
73. 将折叠名片左滑删除按钮直径从 44pt 放大至 48pt，并锁定圆心位置不变 `[done]`
74. 将折叠名片左滑删除按钮直径继续放大至 52pt，并保持圆心锁定 `[done]`
75. 锁定名片夹顶部列表/姓名/公司标签为姓名模式坐标，消除模式切换时的文字位移 `[done]`
76. 将折叠名片删除按钮图标替换为用户提供的 22pt `Trash 3.svg`，展开态保留 `Trash 2.svg` `[done]`
77. 将 AppShell 根背景改为跟随当前页面，并把全局页面切换动画收窄到局部控件，修复页面切换先白后灰的闪烁 `[done]`
78. 为主页面内容加入 0.24 秒 opacity 交叉淡化，并将页面背景从动画内容中分离 `[done]`
79. 移除背景 opacity 渐变，为我的名片与名片夹切换加入灰色背景上下移动及导航 Union 轮廓形变 `[done]`
80. 将创建/编辑名片、“我的名片”、名片夹大面积内容面板统一为名片搜索页的 `SearchBackground` / `#D8D7DB` `[done]`
81. 将名片夹未选中标签置于选中 Union 后方，并让 Union 连续移动以逐步显露底层标签 `[done]`
82. 将“我的名片”进入名片夹时的白色未选中标签移入 AppShell 背景层，移除其 opacity 淡入并改由 Union 变形揭示 `[done]`
83. 将名片夹与“我的名片”的背景位移/Union 形变动画同步到名片夹与搜索/最近添加之间的切换 `[done]`
84. 按 Figma `10592:4348` 将名片夹顶部渐变遮罩保持 x=16、y=178、w=370，更新高度为 47pt `[done]`
85. 将名片夹公司模式改为按公司名称拼音首字母建立 A–Z/# 分组，并在组内按公司拼音排序 `[done]`
86. 重写点击搜索时的底部导航连续形变、双图标汇聚与分段淡出动画，统一使用 S 型贝塞尔曲线 `[done]`
87. 修复姓名/公司模式切换残留与字母标题重合：分组列表内容立即替换；共有标题保留锚点位移动画，无法匹配移动的旧标题先淡出、新标题再淡入，并移除普通滚动及名片展开/收起时的标题动画延迟 `[done]`
88. 重写折叠名片展开动画：折叠态与完整名片复用同一个视图；白底与 0% 底图先由 30pt 跑道圆蒙版裁切，随后高度、圆角、底图和右下按钮槽连续形变；姓名、公司、头像独立移动缩放，其余信息在主形变结束后渐显 `[done]`
89. 按 Figma `10063:3011` 恢复折叠公司与姓名的 15pt/17pt 字体、0 字距及 x=25/y=6.5、x=25/y=30.5 当前坐标；完整名片职位/姓名/拼音组统一下移 18pt，并把展开锚点锁定在卡片左上角、只向下增长 `[done]`
90. 将折叠名片公司信息相对 y 从 5.5pt 下移 1pt 至 6.5pt `[done]`
91. 禁用折叠名片的菜单长按识别，仅允许展开完整名片长按打开菜单，避免抢占列表模式的 `UIDragInteraction` 分组拖动 `[done]`
92. 为折叠名片 `UIDragInteraction` 的 lift 和持续拖动阶段统一提供 370 x 60、30pt `visiblePath` 的自绘 preview，消除白色长方形渐隐后才显示跑道圆的闪变 `[done]`
93. 将完整名片右下信息模块最底部最后一条固定为单行尾部截断，并从换行行数与动态高度计算中排除 `[done]`
94. 调整折叠名片展开底图显露方向：底图按完整展开高度预铺并顶端对齐，由向下增长的蒙版从上往下揭示 `[done]`
95. 修复名片夹姓名/公司模式纵向滚动与左滑删除的手势竞争：删除 pan 在开始前锁定横向，纵向输入直接交给父级滚动视图 `[done]`
96. 按通讯录参考图为姓名/公司右侧索引加入 69 x 58pt 水滴提示、白色大字、当前字母圆底和点击/拖动跟随状态 `[done]`
97. 将姓名/公司索引当前字母圆底由绿色改为纯黑色 `[done]`
98. 完成姓名/公司索引水滴填充色的一轮精确校准 `[done]`
99. 完成姓名/公司索引水滴内部 30pt 大字颜色的一轮调整 `[done]`
100. 将姓名/公司索引水滴最终调整为纯黑底、纯白字 `[done]`
101. 为名片夹三种模式加入顶部标题/操作区、切换栏与 AppShell 面板 Union 随用户滚动连续收起/恢复；固定右侧索引和底部导航，补齐方向防抖、端点吸附、回弹保护及程序化滚动隔离 `[done]`
102. 修复名片夹顶部栏收起后无法恢复：吸顶分组标题改为跟随统一偏移，折叠名片保持原有顶部安全间距，反向用户滚动 1pt 后自动完整展开，离开名片夹或进入搜索时清除隐藏状态 `[done]`
103. 修复分组标题被重复钳制到两个悬挂位置：每帧只允许最后一个越过动态吸顶线的标题成为活动标题，其余标题保持原生坐标 `[done]`
104. 移除姓名/公司分组列表残留的系统 `pinnedViews` 吸顶点，并将唯一活动标题固定在深灰内容框动态 body 上沿下方 24pt，匹配初始首标题边距 `[done]`
105. 修复顶部栏收起时分组标题交接空档：标题身份保持到下一标题真正到达动态吸顶线再原子切换；视觉淡出区间已由第 113 项补充 `[done]`
106. 修复 LazyVStack 回收当前标题锚点造成的偶发吸顶空档：增加独立持久吸顶槽与 retained title 缓存，并验证 A→L→E→O 远距离正反向索引跳转持续有标题 `[done]`
107. 优化名片夹滚动热路径：临时 offset/phase/方向/frame 移出 `@State`，拼音与列表分组加入输入缓存，AppShell 根层不再跟随顶部 offset 每帧重算；反向展开期间取消与手势冲突的同步 ScrollPosition 写入 `[done]`
108. 移除姓名/公司顶部纯色渐变遮罩，替换为 x=16、y=178、w=370、h=47 的顶部渐变背景模糊，并继续随顶部偏移移动到 y=68 `[done]`
109. 重构列表模式吸顶：关闭系统 `pinnedViews`，使用原生 ScrollView 内稳定 `VStack` 与 `visualEffect` 将 53pt 标题行无缝钳制到 y=164...54 的动态深灰 body 顶边，保留标题交互与纵向滚动 `[done]`
110. 将姓名/公司标题下方 370 x 47pt 渐变背景模糊恢复为原有 `SearchBackground` / `#D8D7DB` 50% tint，修复 12% tint 导致的发白色偏 `[done]`
111. 将标题/头像栏上方 402 x 62pt 纯色硬遮罩替换为 `#F3F3F3` 同色渐变背景模糊，并将 CardHolder/AppShell 的 y=62 硬裁切改为羽化 mask，恢复头像与玻璃按钮上方阴影 `[done]`
112. 将字母标题下方 370 x 47pt 区域恢复为真实背景模糊材质：采用 `.systemUltraThinMaterialLight` 实时取样、`SearchBackground / #D8D7DB` 84% 半透明校色及线性 alpha 渐变，移除原 72% 大面积纯色保持区 `[done]`
113. 优化名片夹滚动交接：下一标题距悬顶标题底部 2.5pt 时开始淡出并在中心位置完全透明；顶部导航文字从收起半程开始淡出；安全区模糊层固定在导航层下方；370 x 47pt 模糊层顶部边界改为 100% 不透明同色收束 `[done]`
114. 修复列表模式吸顶行横向缝隙：标题行使用上下各溢出 1pt 的不透明 `SearchBackground` 并在吸顶时隐藏分隔线；进一步确认残留来自跨 Section 合成后压在 header 之上的白色名片底边抗锯齿，最终在整个 ScrollView 前景增加跨越吸顶线上下各 1pt 的全宽同色封口层 `[done]`
115. 修复重复回顶后的标题栏位置漂移：方向 tracking offset 和 delta 改用钳制后的可见滚动位置，顶部负数 overscroll 不再进入下一轮手势基线，触顶时明确重置为 0 `[done]`
116. 完成吸顶标题稳定性收口：顶部 62pt 安全区模糊下沉到滚动内容后方，避免遮住列表模式 y=54...62 的吸顶背景；标题收起值改为基于单次手势固定滚动/标题锚点与当前绝对滚动位置的纯投影，不再对上一帧偏移做累计更新 `[done]`
117. 按最新确认重构列表模式 Sticky Header：每个分类为独立系统 pinned Header，顶部只保留一次 33pt 同色遮罩，恢复 53pt 连续行距；删除 `visualEffect`、动态 offset 与 `listPinnedEdgeSeal`，并将姓名/公司白色标签轮廓提升到列表内容前景，修复灰条遮挡公司标签 `[done]`
118. 为列表模式加入两阶段收起并修复名片穿越顶部 Header：初始 33pt 顶部结构由 7pt pin inset 与 26pt 可滚区域组成，第二阶段整体移动并裁切 ScrollView/Sticky Header 模块；完全收起时模块 top=y54、Header top=y61、名片裁切线 y66 `[done]`
119. 为列表模式补齐与姓名/公司相同的反向滚动立即展开导航：真实用户向页面顶部方向滚动累计 1pt 后完整展开，并从触发位置建立零标题偏移锚点继续纯投影 `[done]`
120. 修复列表模式反向展开后的滚动卡死与反复抽搐：删除展开锁/解锁与绝对 offset 并行竞争，改为单一滚动/标题锚点，并隔离 0.20 秒展开动画产生的 ScrollGeometry 回调 `[done]`
121. 修复列表模式正向收起时导航栏上下抽搐：将 ScrollView 物理视口固定为 820pt，只移动 Sticky Header 模块的视觉位置与裁切，消除动态 containerSize 产生的伪反向 offset `[done]`
122. 将列表模式上下滚动导航交互并入姓名/公司统一状态机：删除列表专用 geometry/phase/方向/锚点分支，共享防抖、回弹保护、反向展开和端点吸附，仅保留 26pt 初始结构行程 `[done]`
123. 修复名片夹连续与快速滚动稳定性：删除基于时间的手势 suppression 和端点吸附 ScrollPosition 回写；Union 与 Sticky Header 共用外部位移，增加 12pt 结构桥接层，并把高频偏移观察隔离到轻量修饰器 `[done]`
124. 修复快速上滑触底后的导航反向抽搐：标题只消费 `.tracking` / `.interacting` 手指阶段的 delta，隔离 `.decelerating` 惯性回弹；新增 UI 测试覆盖慢速拖动、连续 10 轮快速往返、触底回弹与最终按钮交互 `[done]`
125. 将名片夹顶部交互收敛为原生 pan 驱动的二态方向规则：手指上推直接收起、手指下拉直接展开，ScrollGeometry 只记录内容位置，彻底移除 ScrollPhase、连续投影和端点吸附反馈；进入名片夹、跨页面返回及切换模式时强制从展开态开始，并补充返回页面 UI 回归测试 `[done]`
126. 修复名片夹快速切换列表/姓名/公司并快滑时的时序竞态：旧 pan 会话按交互代际失效，分组标题锚点按模式与标题去重过滤；新增逻辑与 UI 压力回归，视觉和动画参数零改动 `[done]`
127. 将名片夹顶部列表/姓名/公司按钮的命中范围从文字 frame 扩展到各自完整 Figma 标签轮廓，并新增三种模式非文字区域点击回归；视觉与动画零改动 `[done]`
128. 完全移除名片夹标题/头像栏上方 402 x 62pt 顶部渐变模糊遮罩，不增加替代材质或色块，并保留标题收起裁切、列表 Header 边界及名片上沿独立渐变模糊 `[done]`
129. 修复列表模式收起状态与顶部导航约束：仅展开列表允许向上收起导航；用户点击收起时同步回到 `y=0` 并展开导航，拖拽来源行与原偏移恢复保持不变，视觉和动画参数零改动 `[done]`
130. 完成名片夹列表多选：顶部按钮切换为 `退出` / `取消选择`，支持跨列表选择折叠名片，含已选名片的列表名称使用 iOS 系统蓝色，并隔离多选态与展开、删除及长按手势 `[done]`
131. 修复进入/退出多选时折叠名片闪烁：保持名片、拖动交互层与左滑手势身份稳定，并关闭模式切换时的卡片级隐式动画 `[done]`
132. 为已选折叠名片增加右上角 iOS 系统绿色圆形白勾，并加入独立的轻量缩放回弹反馈；最终位置相对初版向右、向上各校准 4pt `[done]`
133. 完成多选名片批量换组：使用多个 `UIDragItem` 生成 iOS 风格堆叠拖动预览，并在单次 SwiftData 保存中批量更新名片归属 `[done]`
134. 批量换组保存成功后自动取消实际改变所属列表的名片选择，同时保留多选模式和未改变归属名片的选择；保存失败时完整保留选择 `[done]`
135. 修复名片夹顶部导航文字在快速上下反向滚动后偶发透明：文字透明度改由统一折叠偏移的可中断展示值实时推导，并为连续 10 轮快滑回归加入截图像素可见性断言；布局与动画参数零改动 `[done]`
136. 重写名片夹姓名/公司 → 列表顶部交接：标题以同一视图缩放位移，头像圆连续拉伸为跑道圆并按半程 20% 节点模糊淡出，四字操作文案从重叠/blur=15/透明状态同步散开并在轮廓完成后 0.10 秒清晰，左侧按钮由零尺寸附近放大；补充终态几何、反向恢复、快速切换滚动和底部 Soft Edge 回归 `[done]`
137. 修复列表 → 姓名/公司顶部轮廓终态及连续性：反向主形变直接复用正向 0.56 秒 S 型曲线；右侧原生玻璃改为单一稳定 `.circular` `RoundedRectangle`，从 112 x 46 / 23pt 圆角连续插值到 44 x 44 / 22pt 圆角的完全倒角正方形；玻璃接收层在效果前显式锁定当前宽高，头像与四字文案改为不参与本体布局的 overlay，避免隐藏文案继续扩大取样边界；不再使用 Capsule、Circle 或终点组件替换造成断层；同步更新左侧按钮反向曲线与回归断言 `[done]`
138. 发布前退役 30 条 Excel 演示名片：移除启动自动补样，按演示手机号清理当前 SwiftData、所有本地账户归档，并在旧账户归档和 JSON 备份恢复时继续过滤；保留用户自己的名片、真实收到的名片、列表与账户资料 `[done]`
139. 优化名片夹左滑删除视觉：展开态 `Trash 2.svg` 缩至原尺寸的 80%（22.4pt），红色删除按钮透明度由同一 0...1 滑动进度双向驱动，展开和收起均连续渐变 `[done]`
140. 导入 `color2.png`、`color3.png` 两张名片底图；创建/编辑页顶部预览支持三模板双向分页，并将稳定模板标识贯通 SwiftData、渲染/导出、账户归档/备份与交换 payload；新增双向滑动 UI 回归 `[done]`
141. 优化编辑页底图分页：使用与“我的名片”一致的 402pt 整屏步长和 0.18 秒曲线，只移动底图及同坐标裁切的按钮底色，固定文字/图片/图标前景，并在名片正下方增加无跑道圆三点指示器 `[done]`
142. 恢复接收归类弹窗的取消按钮，同时保持列表区域与弹窗共用玻璃材质；名片夹列表多选时将居中标题实时更新为 `已选择 (N)`，退出后恢复 `名片夹` `[done]`
143. 根据测试编号 `050212` 第二轮 A/B 诊断确认两机均已启动协调器和广播、但递卡上划未进入手势链路；将纵向递卡手势迁移到当前名片稳定命中面并统一画板坐标，新增真实屏幕上划 UI 回归 `[done]`

---

## P0：工程与数据基础

1. 建立正式项目目录结构 `[done]`
- 目标：建立 Models、Views、Components、Services、DesignSystem 等边界。
- 验收：目录结构与 `AGENTS.md` 同步，模板视图不再承载正式业务。

2. 建立名片数据模型 `[done]`
- 目标：用真实业务模型替换模板 `Item`。
- 当前模型：`BusinessCard`、`CardInfoField`、`BusinessCardDraft`、`CardFieldKind`。
- 验收：能表达我的名片、收到的名片、头像、公司 logo、字段类型、排序和三张内置底图的稳定模板标识；旧数据没有模板字段时使用 `color1`。

3. 建立设计系统基础 `[done]`
- 目标：沉淀颜色、字体、玻璃控件、名片尺寸和组件常量。
- 验收：`Designsystem.md` 中的 token 能在 SwiftUI 中复用。

---

## P1：我的名片

1. 我的名片空状态 `[done]`
- Figma：`4268:13366`
- 目标：无名片时展示灰色空卡片。
- 验收：点击灰色卡片进入创建/编辑名片页。

2. 创建/编辑名片页 `[done]`
- Figma：`10599:4144`
- 目标：支持输入姓名、英文名/拼音、公司、职位、信息字段、头像和公司 logo。
- 验收：页面配色按 Figma 使用 `#D8D7DB` 底色、`#F3F3F3` 输入面板、`EditorDefaultAvatar` 本地 SVG 默认头像和白底新增按钮；文本可编辑，未输入时显示 Figma 默认占位文本，姓名默认占位字距为上一版的 1/2 且完整显示；姓名和英文名/拼音单行输入栏不可上下拖动；所有输入键盘使用确认/完成键并可主动收起；字段类型可选择，字段可新增/删除；公司/职位/LOGO 固定信息组独立定位且不复用动态字段行；LOGO 行不偏移；动态信息栏新增/删除按 68pt 改变总高度，文本自动换行每行额外增加 20pt。
- 补充验收：手机号、邮箱、地址和链接字段无论为空、已有内容或重复进入同一字段编辑，每次开始编辑都必须滚动到键盘上方同一固定高度，结束后恢复原位置。

3. 名片实时预览 `[done]`
- 目标：编辑页顶部名片根据输入内容实时更新。
- 验收：头像、公司 logo、字段类型、字段文本变化能反映到预览。

4. 完整名片渲染 `[doing]`
- Figma：`9978:2322`（新版带右侧操作按钮名片）
- 目标：实现 370 x 222 基准名片和动态高度规则。
- 当前：`BusinessCardView` 已改为新版带右侧操作按钮结构；名片底图从 `Group 42.png`、`color2.png`、`color3.png` 中按名片保存的模板标识读取并按按钮槽轮廓裁切，创建/编辑预览可左右分页切换；绘制顺序已调整为先裁切 PNG 高度、再切右下槽、再统一倒角；名片外框与按钮槽均使用普通 `.circular` / 圆弧圆角，不使用 Corner smoothing 或 `.continuous`；当前版本完整名片本体与名片右侧圆形操作按钮均不绘制阴影；“我的名片”页面使用与搜索页一致的 `#D8D7DB` 背景；右下角按钮槽随右侧按钮列裁切，基准槽为 x=329/w=41，仅槽顶两个倒角在宽度冲突时自适应为 20.5pt，其余倒角为 26pt，单按钮 / 单条信息时槽顶相对上一保护位置下移 5.5pt；手机、邮箱、地址、链接图标继续通过本地 SVG 缓存 + SwiftUI `Canvas` 渲染；信息文本列右边缘锁定并底部锚定；左半部分职位、姓名、英文名/拼音和公司文本已回退并锁定为上一版样式；信息栏仍按真实列宽测算换行，每多 1 行名片高度增加 20pt。
- 剩余：需要在模拟器和 Figma 截图中继续做整体视觉校准。

5. 多名片横向循环分页 `[done]`
- Figma：`7575:3207`、`7575:3280`、`7593:14599`
- 目标：多张我的名片可左右滑动循环切换。
- 验收：除底部菜单栏 / 底部导航栏区域外，“我的名片”页面任意位置左右滑动都可切换名片；相邻两张名片保持 32pt 间距；名片跟随手势立即移动；左滑时下一张从右进入且当前名片向左滑出，右滑时上一张从左进入且当前名片向右滑出；底部胶囊提示按名片数量增减；单张名片不显示胶囊；胶囊颜色、尺寸和无阴影规则按 `7575:3207` 实现；当前白色圆点作为独立活动点平滑位移到目标页位置，不再硬切颜色。

6. 添加名片底部弹窗 `[done]`
- Figma：`10681:4430`
- 目标：点击右上角头像后页面变暗，Sheet 初始顶边为 y=409；上拉 grabber 后展开到 y=62。按新稿显示添加名片、登录信息、设置、关联应用与退出登录。
- 当前：内容块使用 50% `SearchBackground`，“添加名片”保持灰色跑道与黑色文字；`设置 / 关联应用` 已合并为 370 x 104pt 两行圆角组件，与红色文字的 `退出登录` 保持 27pt。账户页新增手机号并以其作为本机账户标识，顶部/头像/输入组件间距各收紧 16pt，底部显示“仅本地登录、不可跨设备同步”说明。退出登录已接入确认、按手机号目录归档、清空 SwiftData 与同手机号恢复流程。`设置` 已完成名片交换、名片管理、数据与存储、交互与辅助功能、帮助与关于五组，并按确认移除独立的隐私安全与通知组；偏好已经接入交换、接收、排序、重复处理、删除确认、反馈、动画、字体和翻页行为，数据页支持同手机号 JSON 备份恢复及带确认的本地数据删除。关联应用总览与浏览器/邮箱/地图选择页共用账户 Sheet 内部 `NavigationStack`，不创建二级 Sheet；总览保留 Figma `10924:15888` / `10924:15889` 的 17pt 居中标题、x=16/y=16 的 44pt 圆形返回按钮和 370 x 156pt 三行组件。总览三行与邮箱选择页均移除左侧图标，三行组件下方 27pt 增加红色文字、带原生确认弹窗的 `恢复默认设置`。当前浏览器支持 Safari、Chrome、夸克、QQ浏览器、Edge、UC，并使用本地黑色单色品牌 SVG；邮箱支持邮件、QQ 邮箱、网易邮箱大师、Outlook、Gmail；地图支持 Apple、高德、百度、Google Maps、Waze。仅展示 `canOpenURL` 检测为已安装的软件，偏好只写入 Cardi `UserDefaults`，不可用时降级到系统默认项。
- 交换诊断：`设置 → 帮助与关于 → 交换诊断` 为常驻入口，不再需要连续点击版本号；两台真机可使用同一六位编号和 A/B 标记记录并导出脱敏 JSON，覆盖发现、连接、token、测距/方向、目标选择、意图、传输、持久化与动画，最近保留 5 次 `[done]`
- 验收：点击“添加名片”进入创建/编辑名片页；点击“设置”“关联应用”及任一子项后只发生同一 Sheet 内的水平 push，不创建新的 Sheet、遮罩或 grabber；返回时沿相反方向滑回，原 detent 与拖拽状态保持不变。设置首页只显示已确认的五组，开关与选择可持久化并影响对应业务，备份禁止跨手机号恢复，破坏性操作必须先确认。关联应用总览和邮箱选择页没有左侧图标；浏览器顺序、品牌标志、选择持久化正确；恢复确认的取消不改值、确认同时恢复三项默认值；邮箱、地址和链接按钮按选择跳转，应用卸载或打开失败时正确降级并提示。
- 真机待验：第三方 App URL Scheme 的安装检测与实际唤起必须在安装对应 App 的 iPhone 上完成最终回归；模拟器只验证导航、动态列表、选择持久化和降级逻辑。夸克、QQ浏览器、UC浏览器当前采用“尝试直达，复制网址后降级到 App 主页”的保守路径；QQ 邮箱与网易邮箱大师采用“复制收件人后打开 App”的路径。

7. 我的名片长按菜单 `[done]`
- Figma：`7723:15761`
- 目标：长按名片空白处弹出编辑/保存菜单。
- 验收：可进入编辑页，可保存当前名片为 PNG；点击菜单外空白区域可关闭菜单；菜单出现和消失带有缩放、位移和淡入淡出动画。

---

## P2：名片夹

1. 名字排序模式 `[done]`
- Figma：`10064:3390`
- 目标：没有已保存默认排序偏好时，第一次进入名片夹固定显示姓名页面并按姓名拼音/字母排序；设置重置后仍回到姓名，用户明确保存的其他排序继续优先。
- 验收：折叠名片按姓名拼音首字母分组排序；折叠名片 370 x 60、圆角 30 且使用普通 `.circular` / 圆弧圆角、不透明纯白背景、无阴影且无额外效果，无头像时隐藏右侧头像；折叠态删除按钮为 52pt 红色圆形且锁定圆心不变，展开态删除按钮宽 60pt、上下各留 15pt、圆角 24pt，两种状态统一使用新版 28pt `Trash 2`；删除按钮普通状态隐藏且不可点击，仅进入删除模式后显示，名片回归正常位置后继续隐藏，同一页面只允许一个删除按钮打开，点击后确认再删除；顶部 `名片夹` 使用 34pt 左对齐大标题并显示与“我的名片”统一的右上角账户头像；分段控件按新版 Union path 显示，选中项先拼合面板与标签矩形再统一倒角，且使用普通 `.circular` / 圆弧圆角，不使用 Corner smoothing 或 `.continuous`，未选中项为按 Figma 三态固定映射镜像的白色布尔标签；姓名/公司分组标题使用唯一持久动态吸顶槽，不启用系统 `pinnedViews`，吸顶文字 top 始终比深灰 body 上沿低 24pt，Lazy 回收当前标题时不得出现空档；Figma `渐变遮罩` 展开态位于 x=16、y=178、w=370、h=47，并随顶部收起移动到 y=68，名片超过动态上沿的部分被裁切并在遮罩内自然淡出，顶部标题栏和分组标题不被遮住；右侧索引支持点击和滑动定位；底部只保留最大渐变背景模糊，折叠名片可在底部导航下方继续被模糊渐隐，不得被 y=779 硬截断或额外色块遮挡。

2. 分类模式与公司排序 `[doing]`
- Figma：`10064:3390`、`10064:3656`、`9522:1321`
- 目标：支持向左进入分类模式，向右进入公司排序模式。
- 当前：名片夹三种模式均由原生 pan 手指方向驱动 0/110pt 二态顶部收起；进入页面、跨页面返回和模式切换时始终先展开。列表顶部 26pt 同色区域仅保留结构间距，完全收起时模块 top=y54、Header top=y61、裁切线 y66；列表仍支持添加/改名/删除、展开名片、拖放归属、长按菜单和滚动位置恢复。
- 剩余：左右滑动切换模式和列表重排还未实现。

3. 展开名片 `[done]`
- Figma：`7648:3995`
- 目标：点击折叠名片展开为完整名片。
- 验收：展开名片在原折叠名片位置内联展开，并挤开上下名片，不使用浮层覆盖。

4. 文本复制与图标跳转 `[doing]`
- 目标：文字点击复制，图标点击跳转对应系统软件。
- 当前：名片夹展开完整名片后，右侧手机、邮箱、地址、链接按钮已接入 Figma `10064:3390` / `Group 72` 的自绘固定弹窗。弹窗由 `AppShellView` 绘制在页面最顶层，固定为 x=51、y=675、w=300、h=153，不随名片位置变化；下方渐变背景模糊蒙从 y=675 覆盖到底部并遮住底部菜单栏；弹窗从屏幕底部用 S 型曲线自然滑入。弹窗统一支持 `复制`，并按字段类型提供 `拨打`、`邮件`、`查询`、`前往`：电话打开 `tel://`；邮箱、地址、链接通过统一路由读取 Cardi 内部关联应用偏好并在失败时降级到系统默认项。
- 剩余：文字点击复制尚未接入。
- 验收：电话、邮箱、地址、链接行为分别正确。

5. 名片夹长按菜单：仅展开完整名片可触发，菜单实时定位在目标名片底边下方 16pt；菜单态大幅纵向滑动可继续滚动页面并自动关闭菜单 `[doing]`
- Figma：`7648:4178`
- 目标：长按名片空白处时背景变暗并弹出分享/保存选项。
- 当前：已实现展开名片长按菜单、点击菜单外空白区域关闭菜单、保存 PNG。
- 剩余：分享名片动作尚未实现，遮罩细节仍需视觉校准。

---

## P3：名片搜索

1. 搜索入口展开 `[done]`
- Figma：`4096:14211`
- 目标：点击底部搜索圆球后展开搜索栏，左侧导航收缩。
- 当前：搜索页根背景使用 `SearchBackground` / `#D8D7DB`。
- 验收：页面显示最近一周添加的名片夹折叠名片，只调用名片夹里的名片，按时间从近到远排序；顶部 `最近添加` 按 22pt、x=16、y=71 显示并位于最高层；顶部和底部均使用满强度渐变毛玻璃模糊；折叠名片可点击原位展开；左侧菜单玻璃圆形和右侧搜索栏按最新像素微调。

2. 搜索输入键盘态 `[done]`
- Figma：`7648:3795`
- 目标：再次点击输入框后弹出键盘，输入框左移，右侧出现清空叉号圆圈。
- 验收：点击搜索按钮时先使用奇幻形变同款曲线完整播放 0.68 秒底部导航形变，再无动画交接到搜索控件，退出时反向播放；搜索栏整条任意位置均可进入输入态并聚焦；右下搜索框使用共享几何身份和 0.52 秒同款曲线连续移动到左上 x=16/y=460/w=293/h=62，不允许 opacity 渐隐替换；渐变毛玻璃同步移动到 top=433；左侧菜单按钮消失，右侧叉号圆圈 x=325/y=460/w=61/h=62；非空输入实时进入名片搜索并刷新结果，确认/完成键只收起键盘，叉号清空搜索并返回最近添加。

3. 分类搜索结果 `[done]`
- Figma：`4096:14285`
- 目标：输入文字后按姓名、单位、其他信息分类检索。
- 验收：顶部 `名片搜索` 居中按 22pt 显示；其他信息包含邮箱、电话、地址；搜索结果只调用名片夹里的名片；搜索结果折叠名片可点击原位展开；结果态保留右侧叉号，点击后清空文字并返回最近添加。

4. 搜索退出逻辑 `[done]`
- 目标：点击左侧导航按钮时退出搜索。
- 验收：搜索框收缩为圆球，导航栏恢复胶囊型，页面回到搜索前选中的页面。

---

## P4：名片交换

1. 交换技术方案验证 `[done]`
- 目标：确认第三方 App 可实现的主动上滑递卡与互换方案。
- 当前方案：不调用 AirDrop、NameDrop、二维码或手机碰手机 NFC；前台待机只广播可接收服务，上划后短时主动发现，使用 Nearby Interaction 在 1.5 米内选人。支持方向时按方向与距离筛选，只支持精准距离时仅选唯一且距离明确最近的设备；名片数据通过点对点连接传输。
- 验收：工程已接入 `Services/CardExchange/`，`Info.plist` 已配置本地网络、Bonjour `_carda-ex._tcp` 与 Nearby Interaction 权限说明。

2. 前台上滑递卡与双方互换 `[doing]`
- 前置：双方都打开 Cardi，App 位于前台，停留在“我的名片”页面并各自存在当前名片。
- 目标：单方上划递给 1.5 米内目标明确且最近的人；支持方向时同时要求前方目标，只支持精准距离时要求唯一明确最近；双方 1.5 秒内上划时自动互换。
- 当前：代码已实现上划 20pt 启动短时发现、120pt 提交；纵向递卡由当前名片稳定命中面直接识别并在画板坐标中校验起点，避免整页分页、点击与长按组合吞掉递卡回调。Nearby Interaction 可用性改为按精准距离判断，支持方向时继续使用前方 ±45°，不支持方向时使用唯一最近的 distance-only 降级；两种模式均使用最近 5 次距离中位数、稳定 0.4 秒和多人 0.3 米歧义保护。传输协议支持 `throwIntent`、`delivery`、`mutual`、`returnDelivery`、`persistedAck` 与 `rejected`。未锁定目标时松手会立即回收手势副本并继续逻辑发现，不再把副本冻结在页面顶部；已锁定时继续使用稳定 ID 进入 SwiftUI `distortionEffect` + Metal 连续表面奇幻形变。发送、接收展开、回递与普通收纳保持同一形变体系。
- 持久化：payload 先校验，接收动画结束且 SwiftData 保存成功后才发送 `persistedAck`；发送方收到 ACK 后显示“对方已接住”并震动。
- 生命周期：页面待机仅 advertiser；主动浏览窗口 10 秒；进入后台、离开页面、超时、失败或事务完成后停止主动发现与测距。
- 验证：使用 Xcode 官方 Metal Toolchain 的通用 iOS Simulator构建流程可验证 DEBUG 双向模拟、单向来卡和自动翻面回递。当前回归重点为发送形变中页面原卡保持不动、单向来卡从顶部收束态连续展开并保持同一视图进入可翻转状态、翻面后仅本机名片递出且不再出现下层收纳，以及连续表面形变不出现横向切片层叠。
- 剩余：需要用支持方向的 iPhone 与只返回精准距离的 iPhone 组合，完成真实发现、方向/distance-only、多目标、单向、互换、回递和断线重试测试。

3. 传输层迁移到 Network framework `[research]`
- 原因：iOS 26.5 SDK 已将部分 Multipeer Connectivity API 标为弃用。
- 目标：在不改变交换消息协议、目标选择、持久化 ACK、SwiftData 模型和页面交互的前提下替换发现/连接实现。

4. 上线前交换完整测试 `[doing]`
- 测试计划：`ExchangeTestPlan.md` 已覆盖构建、权限、生命周期、手势、发现、UWB 距离/方向、多人目标、单向、互换、回递、数据、并发、断线、性能、安全与商店发布。
- 已通过：Debug / Release Simulator 构建、generic iOS device arm64 无签名 Release 构建、互换模拟、单向来卡自动翻面回递模拟、4 条交换模拟 UI 自动化（含真实屏幕上划入口与选择现有列表并验证归属）、1 条常驻诊断入口 UI 自动化、目标选择边界与诊断隐私逻辑测试。
- 接收归类：`分到列表` 已改为先显示现有列表单选弹窗；列表区域直接继承弹窗玻璃材质，不显示独立灰色块，并提供取消按钮与外部遮罩关闭；确认后收纳、保存到明确选择的列表并发送 ACK，弹窗期间暂停并重置自动接收计时，无列表时禁用确认。
- 诊断能力：已加入 Release/TestFlight 可用的常驻交换诊断入口、双机测试编号、匿名 peer 摘要、源码位置与事件时间线；开始记录后会自动关闭账户 Sheet、退出搜索并回到“我的名片”，20pt 以上但未进入协调器的上划会记录固定拒绝原因。入口显示、自动返回、开始/结束与导出已有 UI 回归；下一轮双真机测试必须同时导出 A/B 两份 JSON。
- 2026-07-23 首轮真机诊断 `050212`：A 包只有 `recording_started`，没有协调器或广播；B 包只有被动广播，已据此修复诊断开始后仍停留在账户 Sheet 的流程。2026-07-24 第二轮同编号 A/B 包均出现 `diagnostic_return_to_my_cards_requested`、`advertising_started` 与 `exchange_coordinator_started`，但两端都没有 `gesture_primed`、`browsing_started` 或任何 peer 事件，证明新失败发生在 UI 手势入口。纵向递卡现已迁移到当前名片稳定命中面并通过真实屏幕上划 UI 回归；真实双机发现/NI/传输仍需用含本次修复的新 TestFlight 构建复测。
- 已修复 P0：发送事务现有 12 秒 ACK 超时；transport 未连接或发送抛错会显式失败；对端 `.error`、断线与超时会统一清除 intent、outgoing transaction、browser 和 timeout task，后续上划可恢复。
- 仍待修复 P0：最大校验通过 payload 编码后为 16,845,608 字节，超过 14,680,064 字节接收上限；NI 权限拒绝仍缺少终止重试与 Settings 引导。
- Release 可执行文件已确认不含交换模拟入口，但应用包仍包含内部 Markdown 文档；正式归档前需从 Target Resources 排除并复查。
- 剩余：修复其余 P0/P1 后，用支持方向与 distance-only 的真机组合完成真实发现、方向、多目标、单向、互换、回递、权限与断线矩阵；通过前上线状态为 No-Go。

5. App 未打开时接收 `[blocked]`
- 前置：前台双真机链路稳定后，另行评估 Background Modes、Control Center / 锁屏 Control、Live Activity 与通知；当前工程继续保持这些能力关闭。
- 目标：待独立技术验证，不得把前台 advertiser 误认为后台常驻扫描。

6. 未安装 App 的投递 `[blocked]`
- 前置：用户已明确当前近距离交换功能不得调用 AirDrop、NameDrop 或 NFC；后续需要另行确认未安装 App 场景的产品方案。
- 目标：待确认。
- 验收：待确认。

---

## P5：质量与文档

1. 保存名片为 PNG `[done]`
- 目标：将完整名片本体渲染为图片。
- 当前：PNG 已使用独立导出布局，输出完整直角矩形并移除右下按钮槽与按钮圆形底图；字段图标缩小至 75% 且左移 10pt，并根据各信息行真实高度自动对齐对应首行中心；右对齐信息文字列相对屏幕名片总计右移 15pt。
- 验收：输出不包含页面背景、导航、弹窗或遮罩；动态高度正确；屏幕名片、编辑预览与交换视图不受导出专用几何影响。

2. 视觉回归清单 `[todo]`
- 目标：固化我的名片、名片夹、搜索页关键 UI 检查点。
- 验收：每次 UI 改动可按清单复核。

3. 测试策略 `[doing]`
- 目标：覆盖名片布局计算、排序、搜索、保存图片以及交换目标选择、payload、状态机、持久化 ACK 和 UI 动画。
- 当前：交换已新增 `CardExchangeTargetSelectorTests`、`CardExchangePayloadValidationTests`、`CardExchangeDiagnosticsPrivacyTests`、`CardExchangeSimulationUITests`，并覆盖诊断编号规范化、匿名 peer JSON、常驻入口显示、开始后自动返回“我的名片”、结束和可导出状态；完整矩阵见 `ExchangeTestPlan.md`。Coordinator 仍需依赖注入后补齐 send/ACK/断线/权限故障测试。
- 验收：核心纯逻辑可自动测试，交换关键状态可故障注入，双真机端到端证据可复核。

4. 文档同步机制 `[doing]`
- 目标：每次确认新功能或实现变化后同步更新 `AGENTS.md`、`contributing_ai.md`、`Designsystem.md`、`README.md` 或 `ROADMAP.md`。
- 当前：已写入代码与文档同步规则；后续每次代码、资源、设计约束变化时继续执行。
- 验收：文档不再出现与实现相悖的旧描述。

5. 登录账户头像数据源 `[done]`
- 目标：建立真实登录账户资料存储，并将账户头像接入 `AppShellView` 当前统一数据源。
- 当前：登录信息卡片已接入同一账户 Sheet 内的“登录 / 注册 → 修改信息”流程；登录使用手机号+密码，注册增加确认密码，凭据存入仅限本设备的 iOS Keychain。认证后资料页支持 `PhotosPicker` 头像、昵称和邮箱，手机号只读；当前资料同步写入 `@AppStorage` 与账户目录的 `profile.json`。每个规范化手机号另有独立 `cards.json`，完整归档自己的/收到的名片和名片夹列表；退出清空、密码重登、资料回填与两类名片恢复的 UI 自动化已通过。
- 验收：未登录不能跳过认证页；重复注册、错误密码和确认密码不一致会阻止进入资料页；资料保存后所有右上角头像入口显示同一账户头像，并与名片头像保持独立；退出只在归档成功后清空，重新使用同一手机号与密码登录后恢复资料、两类名片及列表归属。

6. iOS 17 版本支持与玻璃 API 降级 `[done]`
- 目标：将 App 最低支持版本扩展到 iOS 17.0，并确保 iOS 26+ Liquid Glass API 不进入低系统运行路径。
- 当前：项目 `IPHONEOS_DEPLOYMENT_TARGET` 已统一为 17.0；所有 `.glassEffect` / `GlassEffectContainer` 调用均保留 `#available(iOS 26.0, *)` 分支，低于 iOS 26 时使用固定浅色 `ultraThinMaterial` / 毛玻璃降级。名片夹与编辑页中原 iOS 18+ 的 `ScrollPosition` / `ScrollGeometry` 已替换为 `ScrollOffsetBridge`，卡片 frame 记录改为 `GeometryReader + PreferenceKey`，左滑删除轴锁定改为 iOS 17 可用的 UIKit pan 附着层。
- 验收：Debug iOS Simulator 构建通过，Release generic iOS arm64 无签名构建通过；编译输出确认目标为 iOS 17.0。

---

## 当前技术债

- `Item.swift` 仍保留在 SwiftData schema 中，仅用于避免模板迁移风险，当前业务不依赖它。
- 名片布局已按 Figma 坐标实现初版，换行高度计算已补齐，但仍需模拟器截图与 Figma 做整体视觉校准。
- 多名片横向分页已可用，动画细节仍需与 Figma 效果继续对齐。
- 底部导航与名片右侧操作按钮图标已移除 WebView 异步加载路径；后续新增本地 SVG 图标应继续复用 `LocalSVGIconView`。
- 当前底图选择已经收敛为创建/编辑页顶部预览窗口内的三页整屏步长滑动，并显示无跑道圆三点指示器；尚未设计独立模板库、在线下载或自定义上传，不能自行扩展。
- 上滑交换前台 MVP 已实现主动手势、1.5 米目标选择、方向/distance-only 双能力路径、单/双向意图、翻面回递与持久化 ACK；当前仍缺修复后双真机 UWB 端到端验证。Multipeer Connectivity 在 iOS 26.5 已进入弃用迁移期，后续需迁移到 Network framework；后台接收、Control、Live Activity、通知与未安装 App 投递仍未实现。
- 交换上线审计中的 ACK 无截止时间、未连接 send 静默丢弃和对端错误不清理事务已修复；合法最大 payload 仍可能超过接收上限，详见 `ExchangeTestPlan.md`。
- 名片夹分享和文本点击复制还未完成；名片夹展开名片的手机、邮箱、地址、链接图标已支持固定弹窗和对应系统跳转。
- 名片列表已具备唯一归属数据结构，列表改名、删除和拖放调整名片归属已完成；列表重排尚未实现。
- 文档同步机制是持续性规则，后续每次改动都需要复查相关 `.md`。
