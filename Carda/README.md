# Carda

Carda 是一个 iOS 社交类名片软件，主要用于名片创建、名片交换与名片管理。

> 协作规则：开发前优先阅读 `AGENTS.md` 与 `contributing_ai.md`。

---

## 产品概览

Carda 的核心对象是“名片”。用户可以创建自己的交互名片，通过类似 AirDrop / 顶部碰一碰的方式递交或交换名片，并在名片夹中管理收到的名片。

主要页面：
- 我的名片
- 名片夹
- 名片搜索

主要能力：
- 创建一张或多张自己的名片。
- 编辑姓名、英文名/拼音、职位、公司、头像、公司 logo 和联系信息。
- 根据输入内容动态生成名片布局。
- 将名片保存为 `.png` 图片。
- 在名片夹中按姓名、分类、单位查看收到的名片。
- 搜索名片，按姓名、单位和其他信息分类展示结果。
- 通过设备碰一碰递交或交换当前展示名片。

---

## 当前状态

更新时间：2026-06-08

当前已确认：
- 项目名为 Carda。
- 产品方向为社交类交互名片软件。
- 当前使用 SwiftUI。
- 当前使用 SwiftData。
- 已建立第一版业务原型结构：模型、设计系统、组件、页面和保存图片服务。
- Figma Page 1 是当前全部页面的视觉来源。
- 名片生成/布局是当前最高复杂度功能。
- 我的名片、创建/编辑名片、名片夹、搜索入口与分类检索已有可运行初版。
- 开发时需要同步维护本地 `.md` 指导文件，避免文档与实现脱节。
- 所有界面的页面背景已统一为 `PageBackground` / `#F9F8FB`。
- 底部导航“我的名片 / 名片夹”图标已改为读取项目本地 `My icon/` 中的 SVG 文件。
- 名片信息栏的手机、邮箱、地址图标已改为读取项目本地 `My icon/Phone.svg`、`My icon/Mail.svg`、`My icon/Map pin.svg`。
- 名片底图已改为读取项目本地 `Card photo/Group 42.png`，并在名片区域居中裁切；底图上方不再叠加半透明覆盖层。
- 名片排版已按最新指令微调：左侧职位、中文姓名、英文名/拼音整体上移 19pt；中文姓名 33pt，并在组内上移 1.5pt；英文名/拼音在组内上移 5pt。
- 全局字体规则已确认：中文使用 PingFang SC，英文/拼音/电话/邮箱/链接使用 SF Pro。
- 创建/编辑名片页已改为左上角返回不保存、右上角确认才提交。
- 创建/编辑名片页默认占位文案已按 Figma `7575:14550` 修正，不再使用开发示例文本；姓名和英文名/拼音占位已按 Figma 字距处理。
- 创建/编辑名片页公司/职位/LOGO 固定信息组已整体重写为独立组件，不复用动态字段行；公司、职位与 LOGO 文本按 Figma 固定坐标重新定位。
- 创建/编辑名片页表单分隔线已统一为左右等距留白，上方固定信息组与下方动态字段组使用同一分隔线颜色。
- 创建/编辑名片页动态信息栏已改为自适应高度：新增/删除字段按 68pt 改变总高度，文本每多换一行额外增加 20pt。
- 我的名片长按菜单支持点击菜单外空白区域关闭。
- 名片夹展开名片的长按菜单支持点击菜单外空白区域关闭。
- 名片夹顶部栏已按 Figma 坐标重写：居中标题、三段分类标签、灰色横线与三态黑色下划线。
- 名片夹折叠名片已按 Figma 修正为 370 x 60、圆角 30、组内 8pt 间距、不透明纯白背景和完整名片同款阴影；无头像时隐藏右侧圆形头像，背景不再叠加其他效果。
- 名片夹姓名模式已改为按姓名拼音首字母 A-Z 分组，并补回右侧字母索引、与我的名片页一致的顶部头像、同色内容背景与底部实时 backdrop blur material 渐变效果。
- 名片夹列表模式顶部显示 `编辑` 与 `添加列表` 两个玻璃胶囊按钮，其中 `编辑` 为 72pt 宽，按钮阴影已减轻；列表模式默认只显示列表行，最顶上的列表行上方不显示分隔线，点击后下拉名片。
- 名片夹点击折叠名片后改为原位置内联展开，展开名片会挤开上下名片，不再使用浮层覆盖。
- 搜索页最近添加会排除自己的名片，只显示收到或保存到名片夹的名片。
- 搜索页顶部标题已按 Figma 三态修正：最近添加态显示最高层 `最近添加` 22pt，搜索结果态显示居中的 `名片搜索` 22pt。
- 最近添加页顶部已加入与底部一致的实时 backdrop blur material 渐变效果，防止列表上滑时遮住标题。
- 搜索页最近添加和搜索结果里的折叠名片均支持点击后原位展开为完整名片，再次点击展开名片收起。
- 搜索页普通展开态底部控件已按最新指令校准：左侧菜单可视玻璃圆半径增加 4.5pt 并右移 0.5pt，右侧搜索栏上移 4pt。
- 搜索键盘态已按 Figma 调整：底部导航栏整体上移到 y=448，左侧菜单按钮消失，搜索框移至 x=16/y=464，右侧显示取消圆圈；整条搜索栏任意位置都能进入输入态并聚焦。
- 搜索页底部已加入实时 backdrop blur material 渐变效果，不叠加颜色填充；键盘态会移动到搜索栏所在区域。
- 已从 `/Users/alex/Desktop/联系人信息.xlsx` 接入 30 条收到的名片示例数据；启动时按“姓名 + 手机号”补齐缺失示例并避免重复插入，用于测试名片夹。

尚未确认或待技术验证：
- AirDrop / 顶部碰一碰交换名片的最终系统实现方案。
- 未打开 App 但已安装 App 时，是否能自动展示 3 秒名片预览。
- 名片底图切换页面尚未设计。
- 名片夹分享、系统跳转、列表编辑/添加列表的最终行为、交换名片仍需继续实现或确认方案。

---

## Figma 来源

文件：`交互名片 Copy`

链接：`https://www.figma.com/design/Vk8sBUsQh05R11tJMJPI32/%E4%BA%A4%E4%BA%92%E5%90%8D%E7%89%87--Copy-?node-id=0-1&t=fJIUVe3NlvYaytkV-1`

File Key：`Vk8sBUsQh05R11tJMJPI32`

说明：以后仅使用该链接作为 Carda 的设计来源。

关键节点：
- `4268:13366`：我的名片空状态
- `7575:14550`：创建/编辑名片
- `9104:1302`：名片文字与信息栏校准参考
- `7597:15587`：我的名片，创建名片后
- `7575:3207`：多名片分页状态与底部胶囊提示
- `7636:17322`：点击头像后的添加名片弹窗
- `7723:15761`：我的名片长按菜单
- `7597:15120`：名片夹名字排序
- `7594:14921`：名片夹分类/单位模式
- `7648:3995`：名片夹展开名片
- `7648:4178`：名片夹长按菜单
- `4096:14211`：搜索入口展开
- `7648:3795`：搜索输入键盘态
- `4096:14285`：搜索结果分类

---

## 技术栈

- Swift
- SwiftUI
- SwiftData
- Xcode

后续可能涉及但尚未确认：
- ShareLink / AirDrop / Nearby Interaction：名片交换。
- URL schemes：点击电话、邮箱、地址、链接图标后的系统跳转。

---

## 当前项目结构

- `CardaApp.swift`：应用入口与 SwiftData 容器。
- `ContentView.swift`：进入 `AppShellView`。
- `Item.swift`：模板遗留模型，暂保留在 SwiftData schema 中以避免迁移风险。
- `Models/`：名片、字段、草稿和字段类型。
- `Views/MyCards/`：我的名片、创建/编辑名片。
- `Views/CardHolder/`：名片夹。
- `Views/Search/`：名片搜索。
- `Components/`：名片渲染、折叠名片、底部导航、头像等复用组件。
- `Services/`：保存名片图片等服务。
- `DesignSystem/`：颜色、尺寸、玻璃样式和名片常量。
- `My icon/`：底部导航 tab 与名片信息栏的 SVG 图标源文件。
- `Card photo/`：名片底图 PNG 资源。
- `Assets.xcassets/`：资源目录。
- `AGENTS.md`：项目级 AI 指导规则。
- `contributing_ai.md`：AI 协作与变更协议。
- `Designsystem.md`：设计系统记录。
- `ROADMAP.md`：路线图与待确认事项。

---

## 运行方式

1. 使用 Xcode 打开项目。
2. 选择目标设备或模拟器。
3. Build & Run。

命令行构建：

```bash
xcodebuild -project ../Carda.xcodeproj -scheme Carda -destination 'generic/platform=iOS Simulator' build
```

---

## 开发协作

开发时必须优先参考：

1. `AGENTS.md`
2. `contributing_ai.md`
3. `Designsystem.md`
4. `ROADMAP.md`
5. Figma Page 1 对应 frame

如果描述不明确，AI 必须先询问并等待确认。

代码与文档同步规则：
- 按用户指令修改代码、资源或设计规则时，应同时更新相关 `.md` 文件。
- 产品长期规则写入 `AGENTS.md`。
- AI 协作流程写入 `contributing_ai.md`。
- 设计系统、图标来源、布局约束写入 `Designsystem.md`。
- 当前状态和路线图写入 `README.md` 与 `ROADMAP.md`。

---

## 许可

TBD
